import std.db
import db.{init_db}
import commands.{sql_escape, escape_like_pattern, try_resolve_id, try_resolve_project_id, build_tag_clauses, build_project_tag_clauses, build_project_clause, resolve_project_id, list_contains, cmd_ready, cmd_ls, cmd_add, cmd_edit, cmd_done, cmd_start, cmd_dep_add, cmd_graph, cmd_project_add, cmd_project_ls, cmd_project_show, cmd_project_tag, cmd_project_untag, cmd_project_rm, set_task_status, has_cycle, is_task_id_taken, generate_unique_task_id, run_transition_batch}
import taskid.{is_valid_id}
import std.testing.{capture_print}

fn setup_test_db() {
    init_db()
}

test "sql_escape basic string" {
    assert_eq(sql_escape("hello"), "hello")
}

test "sql_escape single quotes" {
    assert_eq(sql_escape("it's a test"), "it''s a test")
}

test "sql_escape empty string" {
    assert_eq(sql_escape(""), "")
}

test "build_tag_clauses empty list" {
    let clauses = build_tag_clauses([])
    assert_eq(clauses.len(), 0)
}

test "build_tag_clauses single tag" {
    let clauses = build_tag_clauses(["bug"])
    assert_eq(clauses.len(), 1)
    assert((clauses.get(0) ?? "").contains("tg.tag = 'bug'"))
}

test "build_tag_clauses multiple tags" {
    let clauses = build_tag_clauses(["bug", "urgent"])
    assert_eq(clauses.len(), 2)
    assert((clauses.get(0) ?? "").contains("tg.tag = 'bug'"))
    assert((clauses.get(1) ?? "").contains("tg.tag = 'urgent'"))
}

test "build_tag_clauses tag with quote" {
    let clauses = build_tag_clauses(["it's"])
    assert_eq(clauses.len(), 1)
    assert((clauses.get(0) ?? "").contains("tg.tag = 'it''s'"))
}

test "build_project_tag_clauses empty list" {
    let clauses = build_project_tag_clauses([])
    assert_eq(clauses.len(), 0)
}

test "build_project_tag_clauses single tag" {
    let clauses = build_project_tag_clauses(["team:backend"])
    assert_eq(clauses.len(), 1)
    assert((clauses.get(0) ?? "").contains("pt.tag = 'team:backend'"))
    assert((clauses.get(0) ?? "").contains("pt.project_id = p.id"))
}

test "build_project_tag_clauses multiple tags" {
    let clauses = build_project_tag_clauses(["team:backend", "q3"])
    assert_eq(clauses.len(), 2)
    assert((clauses.get(0) ?? "").contains("pt.tag = 'team:backend'"))
    assert((clauses.get(1) ?? "").contains("pt.tag = 'q3'"))
}

test "build_project_tag_clauses tag with quote" {
    let clauses = build_project_tag_clauses(["it's"])
    assert_eq(clauses.len(), 1)
    assert((clauses.get(0) ?? "").contains("pt.tag = 'it''s'"))
}

test "list_contains found" {
    assert(list_contains(["a", "b", "c"], "b"))
}

test "list_contains not found" {
    assert(!list_contains(["a", "b", "c"], "z"))
}

test "list_contains empty list" {
    assert(!list_contains([], "a"))
}

test "cmd_ready shows open tasks" {
    with db.connect(":memory:") {
        setup_test_db()
        db.exec("INSERT INTO tasks (id, title, status, priority) VALUES ('TASK0001', 'Open task', 'open', 2)").unwrap()
        let mut output: List[Str] = []
        with capture_print(output) {
            cmd_ready([], "", false)
        }
        assert_eq(output.len(), 1)
        assert((output.get(0) ?? "").contains("Open task"))
    }
}

test "cmd_ready shows in_progress tasks" {
    with db.connect(":memory:") {
        setup_test_db()
        db.exec("INSERT INTO tasks (id, title, status, priority) VALUES ('TASK0001', 'Active task', 'in_progress', 2)").unwrap()
        let mut output: List[Str] = []
        with capture_print(output) {
            cmd_ready([], "", false)
        }
        assert_eq(output.len(), 1)
        assert((output.get(0) ?? "").contains("Active task"))
    }
}

test "cmd_ready excludes done and cancelled" {
    with db.connect(":memory:") {
        setup_test_db()
        db.exec("INSERT INTO tasks (id, title, status, priority) VALUES ('TASK0001', 'Done task', 'done', 2)").unwrap()
        db.exec("INSERT INTO tasks (id, title, status, priority) VALUES ('TASK0002', 'Cancelled task', 'cancelled', 2)").unwrap()
        let mut output: List[Str] = []
        with capture_print(output) {
            cmd_ready([], "", false)
        }
        assert_eq(output.len(), 1)
        assert((output.get(0) ?? "").contains("No ready tasks."))
    }
}

test "cmd_ready excludes blocked tasks" {
    with db.connect(":memory:") {
        setup_test_db()
        db.exec("INSERT INTO tasks (id, title, status, priority) VALUES ('BLOCKER1', 'Blocker', 'open', 1)").unwrap()
        db.exec("INSERT INTO tasks (id, title, status, priority) VALUES ('BLOCKED1', 'Blocked', 'open', 2)").unwrap()
        db.exec("INSERT INTO deps (blocker_id, blocked_id) VALUES ('BLOCKER1', 'BLOCKED1')").unwrap()
        let mut output: List[Str] = []
        with capture_print(output) {
            cmd_ready([], "", false)
        }
        assert_eq(output.len(), 1)
        assert((output.get(0) ?? "").contains("Blocker"))
        assert(!((output.get(0) ?? "").contains("Blocked")))
    }
}

test "cmd_ready includes task whose blocker is done" {
    with db.connect(":memory:") {
        setup_test_db()
        db.exec("INSERT INTO tasks (id, title, status, priority) VALUES ('BLOCKER1', 'Blocker', 'done', 1)").unwrap()
        db.exec("INSERT INTO tasks (id, title, status, priority) VALUES ('BLOCKED1', 'Unblocked now', 'open', 2)").unwrap()
        db.exec("INSERT INTO deps (blocker_id, blocked_id) VALUES ('BLOCKER1', 'BLOCKED1')").unwrap()
        let mut output: List[Str] = []
        with capture_print(output) {
            cmd_ready([], "", false)
        }
        assert_eq(output.len(), 1)
        assert((output.get(0) ?? "").contains("Unblocked now"))
    }
}

test "cmd_ready includes task whose blocker is cancelled" {
    with db.connect(":memory:") {
        setup_test_db()
        db.exec("INSERT INTO tasks (id, title, status, priority) VALUES ('BLOCKER1', 'Blocker', 'cancelled', 1)").unwrap()
        db.exec("INSERT INTO tasks (id, title, status, priority) VALUES ('BLOCKED1', 'Unblocked now', 'open', 2)").unwrap()
        db.exec("INSERT INTO deps (blocker_id, blocked_id) VALUES ('BLOCKER1', 'BLOCKED1')").unwrap()
        let mut output: List[Str] = []
        with capture_print(output) {
            cmd_ready([], "", false)
        }
        assert_eq(output.len(), 1)
        assert((output.get(0) ?? "").contains("Unblocked now"))
    }
}

test "cmd_add creates a task" {
    with db.connect(":memory:") {
        setup_test_db()
        let mut output: List[Str] = []
        with capture_print(output) {
            cmd_add("My new task", "A description", 1, true, ["bug", "urgent"], "", "")
        }
        assert_eq(output.len(), 2)
        assert((output.get(0) ?? "").contains("Created:"))
        assert((output.get(0) ?? "").contains("My new task"))
        assert((output.get(1) ?? "").contains("P1"))
        assert(!(output.get(1) ?? "").contains("defaulted"))

        let rows = db.query("SELECT id, title, description, priority FROM tasks").unwrap()
        assert_eq(rows.len(), 1)
        let row = rows.get(0).unwrap()
        assert_eq(row.get_at(1) ?? "", "My new task")
        assert_eq(row.get_at(2) ?? "", "A description")
        assert_eq(row.get_at(3) ?? "", "1")

        let tag_rows = db.query("SELECT tag FROM tags ORDER BY tag ASC").unwrap()
        assert_eq(tag_rows.len(), 2)
        assert_eq(tag_rows.get(0).unwrap().get_at(0) ?? "", "bug")
        assert_eq(tag_rows.get(1).unwrap().get_at(0) ?? "", "urgent")
    }
}

test "cmd_add flags a defaulted priority" {
    with db.connect(":memory:") {
        setup_test_db()
        let mut output: List[Str] = []
        with capture_print(output) {
            cmd_add("Untriaged", "", 2, false, [], "", "")
        }
        assert_eq(output.len(), 2)
        assert((output.get(1) ?? "").contains("P2"))
        assert((output.get(1) ?? "").contains("defaulted"))
    }
}

test "cmd_done marks task done" {
    with db.connect(":memory:") {
        setup_test_db()
        db.exec("INSERT INTO tasks (id, title, status, priority) VALUES ('TASK0001', 'To finish', 'open', 2)").unwrap()
        let mut output: List[Str] = []
        with capture_print(output) {
            cmd_done("TASK0001", "")
        }
        let rows = db.query("SELECT status FROM tasks WHERE id = 'TASK0001'").unwrap()
        assert_eq(rows.get(0).unwrap().get_at(0) ?? "", "done")
    }
}

test "cmd_start marks task in_progress" {
    with db.connect(":memory:") {
        setup_test_db()
        db.exec("INSERT INTO tasks (id, title, status, priority) VALUES ('TASK0001', 'To start', 'open', 2)").unwrap()
        let mut output: List[Str] = []
        with capture_print(output) {
            cmd_start("TASK0001", "")
        }
        let rows = db.query("SELECT status FROM tasks WHERE id = 'TASK0001'").unwrap()
        assert_eq(rows.get(0).unwrap().get_at(0) ?? "", "in_progress")
    }
}

test "run_transition_batch closes all good ids and reports zero failures" {
    with db.connect(":memory:") {
        setup_test_db()
        db.exec("INSERT INTO tasks (id, title, status, priority) VALUES ('taska001', 'A', 'open', 2)").unwrap()
        db.exec("INSERT INTO tasks (id, title, status, priority) VALUES ('taskb001', 'B', 'open', 2)").unwrap()
        db.exec("INSERT INTO tasks (id, title, status, priority) VALUES ('taskc001', 'C', 'open', 2)").unwrap()
        let mut output: List[Str] = []
        let mut failures = 0
        with capture_print(output) {
            failures = run_transition_batch(["taska001", "taskb001", "taskc001"], "done", "closed", "")
        }
        assert_eq(failures, 0)
        let rows = db.query("SELECT status FROM tasks ORDER BY id ASC").unwrap()
        assert_eq(rows.len(), 3)
        for row in rows {
            assert_eq(row.get_at(0) ?? "", "done")
        }
    }
}

test "run_transition_batch is best-effort around a bad id in the middle" {
    with db.connect(":memory:") {
        setup_test_db()
        db.exec("INSERT INTO tasks (id, title, status, priority) VALUES ('taska001', 'A', 'open', 2)").unwrap()
        db.exec("INSERT INTO tasks (id, title, status, priority) VALUES ('taskc001', 'C', 'open', 2)").unwrap()
        let mut output: List[Str] = []
        let mut failures = 0
        with capture_print(output) {
            failures = run_transition_batch(["taska001", "nosuchid", "taskc001"], "done", "closed", "")
        }
        assert_eq(failures, 1)
        let a_row = db.query_one("SELECT status FROM tasks WHERE id = 'taska001'").unwrap().unwrap()
        assert_eq(a_row.get_at(0) ?? "", "done")
        let c_row = db.query_one("SELECT status FROM tasks WHERE id = 'taskc001'").unwrap().unwrap()
        assert_eq(c_row.get_at(0) ?? "", "done")
    }
}

test "run_transition_batch counts an ambiguous prefix as a failure" {
    with db.connect(":memory:") {
        setup_test_db()
        db.exec("INSERT INTO tasks (id, title, status, priority) VALUES ('taska001', 'A', 'open', 2)").unwrap()
        db.exec("INSERT INTO tasks (id, title, status, priority) VALUES ('taska002', 'A2', 'open', 2)").unwrap()
        db.exec("INSERT INTO tasks (id, title, status, priority) VALUES ('taskc001', 'C', 'open', 2)").unwrap()
        let mut output: List[Str] = []
        let mut failures = 0
        with capture_print(output) {
            failures = run_transition_batch(["taska", "taskc001"], "done", "closed", "")
        }
        assert_eq(failures, 1)
        let c_row = db.query_one("SELECT status FROM tasks WHERE id = 'taskc001'").unwrap().unwrap()
        assert_eq(c_row.get_at(0) ?? "", "done")
        let a1_row = db.query_one("SELECT status FROM tasks WHERE id = 'taska001'").unwrap().unwrap()
        assert_eq(a1_row.get_at(0) ?? "", "open")
        let a2_row = db.query_one("SELECT status FROM tasks WHERE id = 'taska002'").unwrap().unwrap()
        assert_eq(a2_row.get_at(0) ?? "", "open")
    }
}

test "set_task_status to done sets closed_at" {
    with db.connect(":memory:") {
        setup_test_db()
        db.exec("INSERT INTO tasks (id, title, status, priority) VALUES ('TASK0001', 'T', 'open', 2)").unwrap()
        set_task_status("TASK0001", "done")
        let row = db.query_one("SELECT status, closed_at FROM tasks WHERE id = 'TASK0001'").unwrap().unwrap()
        assert_eq(row.get_at(0) ?? "", "done")
        assert(!(row.get_at(1) ?? "").is_empty())
    }
}

test "set_task_status reopen clears closed_at" {
    with db.connect(":memory:") {
        setup_test_db()
        db.exec("INSERT INTO tasks (id, title, status, priority, closed_at) VALUES ('TASK0001', 'T', 'done', 2, '2026-01-01T00:00:00Z')").unwrap()
        set_task_status("TASK0001", "open")
        let row = db.query_one("SELECT status, closed_at FROM tasks WHERE id = 'TASK0001'").unwrap().unwrap()
        assert_eq(row.get_at(0) ?? "", "open")
        assert_eq(row.get_at(1) ?? "", "")
    }
}

test "has_cycle detects transitive cycle" {
    with db.connect(":memory:") {
        setup_test_db()
        db.exec("INSERT INTO tasks (id, title, status, priority) VALUES ('TASKA001', 'A', 'open', 2)").unwrap()
        db.exec("INSERT INTO tasks (id, title, status, priority) VALUES ('TASKB001', 'B', 'open', 2)").unwrap()
        db.exec("INSERT INTO tasks (id, title, status, priority) VALUES ('TASKC001', 'C', 'open', 2)").unwrap()
        let mut output: List[Str] = []
        with capture_print(output) {
            cmd_dep_add("TASKA001", "TASKB001")
            cmd_dep_add("TASKB001", "TASKC001")
        }
        // Adding C -> A next would close A -> B -> C -> A into a cycle.
        // This is the exact predicate cmd_dep_add checks before inserting;
        // exercised directly here because the rejecting branch calls exit(1),
        // which would abort the whole test binary if hit through cmd_dep_add.
        assert(has_cycle("TASKC001", "TASKA001"))
    }
}

test "cmd_dep_add accepts diamond dependency" {
    with db.connect(":memory:") {
        setup_test_db()
        db.exec("INSERT INTO tasks (id, title, status, priority) VALUES ('TASKA001', 'A', 'open', 2)").unwrap()
        db.exec("INSERT INTO tasks (id, title, status, priority) VALUES ('TASKB001', 'B', 'open', 2)").unwrap()
        db.exec("INSERT INTO tasks (id, title, status, priority) VALUES ('TASKC001', 'C', 'open', 2)").unwrap()
        db.exec("INSERT INTO tasks (id, title, status, priority) VALUES ('TASKD001', 'D', 'open', 2)").unwrap()
        let mut output: List[Str] = []
        with capture_print(output) {
            cmd_dep_add("TASKA001", "TASKB001")
            cmd_dep_add("TASKA001", "TASKC001")
            cmd_dep_add("TASKB001", "TASKD001")
            cmd_dep_add("TASKC001", "TASKD001")
        }
        assert_eq(output.len(), 4)
        for line in output {
            assert(line.contains("blocks"))
        }
        let rows = db.query("SELECT blocker_id, blocked_id FROM deps").unwrap()
        assert_eq(rows.len(), 4)
    }
}

test "cmd_dep_add creates dependency" {
    with db.connect(":memory:") {
        setup_test_db()
        db.exec("INSERT INTO tasks (id, title, status, priority) VALUES ('TASK0001', 'Blocker', 'open', 1)").unwrap()
        db.exec("INSERT INTO tasks (id, title, status, priority) VALUES ('TASK0002', 'Blocked', 'open', 2)").unwrap()
        let mut output: List[Str] = []
        with capture_print(output) {
            cmd_dep_add("TASK0001", "TASK0002")
        }
        assert_eq(output.len(), 1)
        assert((output.get(0) ?? "").contains("blocks"))

        let rows = db.query("SELECT blocker_id, blocked_id FROM deps").unwrap()
        assert_eq(rows.len(), 1)
        assert_eq(rows.get(0).unwrap().get_at(0) ?? "", "TASK0001")
        assert_eq(rows.get(0).unwrap().get_at(1) ?? "", "TASK0002")
    }
}

// --- Projects ---

fn project_id_by_name(name: Str) -> Str {
    let row = db.query_one("SELECT id FROM projects WHERE name = {name}").unwrap().unwrap()
    row.get_at(0) ?? ""
}

test "init_db adds project_id column to tasks" {
    with db.connect(":memory:") {
        setup_test_db()
        let cols = db.query("PRAGMA table_info(tasks)").unwrap()
        let mut has_project_id = false
        for col in cols {
            if (col.get_at(1) ?? "") == "project_id" {
                has_project_id = true
            }
        }
        assert(has_project_id)
    }
}

test "init_db is idempotent" {
    with db.connect(":memory:") {
        setup_test_db()
        setup_test_db()
        let cols = db.query("PRAGMA table_info(tasks)").unwrap()
        let mut project_id_count = 0
        for col in cols {
            if (col.get_at(1) ?? "") == "project_id" {
                project_id_count = project_id_count + 1
            }
        }
        assert_eq(project_id_count, 1)
    }
}

test "build_project_clause empty ref" {
    with db.connect(":memory:") {
        setup_test_db()
        assert_eq(build_project_clause(""), "")
    }
}

test "build_project_clause resolves to project id" {
    with db.connect(":memory:") {
        setup_test_db()
        db.exec("INSERT INTO projects (id, name) VALUES ('PROJ0001', 'launch')").unwrap()
        let clause = build_project_clause("launch")
        assert(clause.contains("t.project_id = 'PROJ0001'"))
    }
}

test "resolve_project_id by exact name" {
    with db.connect(":memory:") {
        setup_test_db()
        db.exec("INSERT INTO projects (id, name) VALUES ('PROJ0001', 'launch')").unwrap()
        assert_eq(resolve_project_id("launch"), "PROJ0001")
    }
}

test "resolve_project_id by id prefix" {
    with db.connect(":memory:") {
        setup_test_db()
        db.exec("INSERT INTO projects (id, name) VALUES ('proj0001', 'launch')").unwrap()
        assert_eq(resolve_project_id("proj00"), "proj0001")
    }
}

test "cmd_project_add creates project" {
    with db.connect(":memory:") {
        setup_test_db()
        let mut output: List[Str] = []
        with capture_print(output) {
            cmd_project_add("v2-launch", "Ship v2", [])
        }
        let rows = db.query("SELECT name, description, status FROM projects").unwrap()
        assert_eq(rows.len(), 1)
        let row = rows.get(0).unwrap()
        assert_eq(row.get_at(0) ?? "", "v2-launch")
        assert_eq(row.get_at(1) ?? "", "Ship v2")
        assert_eq(row.get_at(2) ?? "", "active")
    }
}

test "cmd_project_add with tags inserts project_tags" {
    with db.connect(":memory:") {
        setup_test_db()
        let mut output: List[Str] = []
        with capture_print(output) {
            cmd_project_add("API Rewrite", "", ["team:backend", "q3"])
        }
        let pid = project_id_by_name("API Rewrite")
        let tag_rows = db.query("SELECT tag FROM project_tags WHERE project_id = {pid} ORDER BY tag ASC").unwrap()
        assert_eq(tag_rows.len(), 2)
        assert_eq(tag_rows.get(0).unwrap().get_at(0) ?? "", "q3")
        assert_eq(tag_rows.get(1).unwrap().get_at(0) ?? "", "team:backend")
    }
}

test "cmd_project_tag adds project_tags rows" {
    with db.connect(":memory:") {
        setup_test_db()
        db.exec("INSERT INTO projects (id, name) VALUES ('PROJ0001', 'launch')").unwrap()
        let mut output: List[Str] = []
        with capture_print(output) {
            cmd_project_tag("launch", ["urgent", "team:backend"])
        }
        let tag_rows = db.query("SELECT tag FROM project_tags WHERE project_id = 'PROJ0001' ORDER BY tag ASC").unwrap()
        assert_eq(tag_rows.len(), 2)
        assert_eq(tag_rows.get(0).unwrap().get_at(0) ?? "", "team:backend")
        assert_eq(tag_rows.get(1).unwrap().get_at(0) ?? "", "urgent")
    }
}

test "cmd_project_untag removes project_tags rows" {
    with db.connect(":memory:") {
        setup_test_db()
        db.exec("INSERT INTO projects (id, name) VALUES ('PROJ0001', 'launch')").unwrap()
        db.exec("INSERT INTO project_tags (project_id, tag) VALUES ('PROJ0001', 'urgent')").unwrap()
        db.exec("INSERT INTO project_tags (project_id, tag) VALUES ('PROJ0001', 'q3')").unwrap()
        let mut output: List[Str] = []
        with capture_print(output) {
            cmd_project_untag("launch", ["q3"])
        }
        let tag_rows = db.query("SELECT tag FROM project_tags WHERE project_id = 'PROJ0001'").unwrap()
        assert_eq(tag_rows.len(), 1)
        assert_eq(tag_rows.get(0).unwrap().get_at(0) ?? "", "urgent")
    }
}

test "cmd_project_ls tag filter returns only tagged projects" {
    with db.connect(":memory:") {
        setup_test_db()
        db.exec("INSERT INTO projects (id, name) VALUES ('PROJ0001', 'backend-work')").unwrap()
        db.exec("INSERT INTO projects (id, name) VALUES ('PROJ0002', 'frontend-work')").unwrap()
        db.exec("INSERT INTO project_tags (project_id, tag) VALUES ('PROJ0001', 'team:backend')").unwrap()
        let mut output: List[Str] = []
        with capture_print(output) {
            cmd_project_ls(false, ["team:backend"])
        }
        assert_eq(output.len(), 1)
        assert((output.get(0) ?? "").contains("backend-work"))
        assert(!((output.get(0) ?? "").contains("frontend-work")))
    }
}

test "cmd_project_ls shows project tags" {
    with db.connect(":memory:") {
        setup_test_db()
        db.exec("INSERT INTO projects (id, name) VALUES ('PROJ0001', 'backend-work')").unwrap()
        db.exec("INSERT INTO project_tags (project_id, tag) VALUES ('PROJ0001', 'team:backend')").unwrap()
        let mut output: List[Str] = []
        with capture_print(output) {
            cmd_project_ls(false, [])
        }
        assert_eq(output.len(), 1)
        assert((output.get(0) ?? "").contains("team:backend"))
    }
}

test "cmd_project_rm cascades project_tags" {
    with db.connect(":memory:") {
        setup_test_db()
        db.exec("INSERT INTO projects (id, name) VALUES ('PROJ0001', 'launch')").unwrap()
        db.exec("INSERT INTO project_tags (project_id, tag) VALUES ('PROJ0001', 'urgent')").unwrap()
        let mut output: List[Str] = []
        with capture_print(output) {
            cmd_project_rm("launch")
        }
        let tag_rows = db.query("SELECT tag FROM project_tags WHERE project_id = 'PROJ0001'").unwrap()
        assert_eq(tag_rows.len(), 0)
    }
}

test "cmd_project_show renders Tags line" {
    with db.connect(":memory:") {
        setup_test_db()
        db.exec("INSERT INTO projects (id, name) VALUES ('PROJ0001', 'launch')").unwrap()
        db.exec("INSERT INTO project_tags (project_id, tag) VALUES ('PROJ0001', 'team:backend')").unwrap()
        db.exec("INSERT INTO project_tags (project_id, tag) VALUES ('PROJ0001', 'urgent')").unwrap()
        let mut output: List[Str] = []
        with capture_print(output) {
            cmd_project_show("launch", false)
        }
        let joined = output.join("\n")
        assert(joined.contains("Tags:"))
        assert(joined.contains("team:backend"))
        assert(joined.contains("urgent"))
    }
}

test "cmd_add assigns project" {
    with db.connect(":memory:") {
        setup_test_db()
        db.exec("INSERT INTO projects (id, name) VALUES ('PROJ0001', 'launch')").unwrap()
        let mut output: List[Str] = []
        with capture_print(output) {
            cmd_add("Member task", "", 2, true, [], "launch", "")
        }
        let rows = db.query("SELECT project_id FROM tasks").unwrap()
        assert_eq(rows.len(), 1)
        assert_eq(rows.get(0).unwrap().get_at(0) ?? "", "PROJ0001")
    }
}

test "cmd_edit reassigns project" {
    with db.connect(":memory:") {
        setup_test_db()
        db.exec("INSERT INTO projects (id, name) VALUES ('PROJ0001', 'launch')").unwrap()
        db.exec("INSERT INTO tasks (id, title, status, priority) VALUES ('TASK0001', 'T', 'open', 2)").unwrap()
        let mut output: List[Str] = []
        with capture_print(output) {
            cmd_edit("TASK0001", "", "", -1, "", "launch", false, false)
        }
        let rows = db.query("SELECT project_id FROM tasks WHERE id = 'TASK0001'").unwrap()
        assert_eq(rows.get(0).unwrap().get_at(0) ?? "", "PROJ0001")
    }
}

test "cmd_edit clears project" {
    with db.connect(":memory:") {
        setup_test_db()
        db.exec("INSERT INTO projects (id, name) VALUES ('PROJ0001', 'launch')").unwrap()
        db.exec("INSERT INTO tasks (id, title, status, priority, project_id) VALUES ('TASK0001', 'T', 'open', 2, 'PROJ0001')").unwrap()
        let mut output: List[Str] = []
        with capture_print(output) {
            cmd_edit("TASK0001", "", "", -1, "", "", true, false)
        }
        let row = db.query_one("SELECT project_id FROM tasks WHERE id = 'TASK0001'").unwrap().unwrap()
        assert_eq(row.get_at(0) ?? "", "")
    }
}

test "cmd_project_rm unlinks member tasks" {
    with db.connect(":memory:") {
        setup_test_db()
        db.exec("INSERT INTO projects (id, name) VALUES ('PROJ0001', 'launch')").unwrap()
        db.exec("INSERT INTO tasks (id, title, status, priority, project_id) VALUES ('TASK0001', 'Member', 'open', 2, 'PROJ0001')").unwrap()
        let mut output: List[Str] = []
        with capture_print(output) {
            cmd_project_rm("launch")
        }
        let proj_rows = db.query("SELECT id FROM projects").unwrap()
        assert_eq(proj_rows.len(), 0)
        let task_rows = db.query("SELECT id FROM tasks WHERE id = 'TASK0001'").unwrap()
        assert_eq(task_rows.len(), 1)
        let row = db.query_one("SELECT project_id FROM tasks WHERE id = 'TASK0001'").unwrap().unwrap()
        assert_eq(row.get_at(0) ?? "", "")
    }
}

test "cmd_ls project filter returns only members" {
    with db.connect(":memory:") {
        setup_test_db()
        db.exec("INSERT INTO projects (id, name) VALUES ('PROJ0001', 'launch')").unwrap()
        db.exec("INSERT INTO tasks (id, title, status, priority, project_id) VALUES ('TASK0001', 'Member', 'open', 2, 'PROJ0001')").unwrap()
        db.exec("INSERT INTO tasks (id, title, status, priority) VALUES ('TASK0002', 'Outsider', 'open', 2)").unwrap()
        let mut output: List[Str] = []
        with capture_print(output) {
            cmd_ls("", [], "launch", false, false, false)
        }
        assert_eq(output.len(), 1)
        assert((output.get(0) ?? "").contains("Member"))
        assert(!((output.get(0) ?? "").contains("Outsider")))
    }
}

test "cmd_ready project filter returns only members" {
    with db.connect(":memory:") {
        setup_test_db()
        db.exec("INSERT INTO projects (id, name) VALUES ('PROJ0001', 'launch')").unwrap()
        db.exec("INSERT INTO tasks (id, title, status, priority, project_id) VALUES ('TASK0001', 'Member', 'open', 2, 'PROJ0001')").unwrap()
        db.exec("INSERT INTO tasks (id, title, status, priority) VALUES ('TASK0002', 'Outsider', 'open', 2)").unwrap()
        let mut output: List[Str] = []
        with capture_print(output) {
            cmd_ready([], "launch", false)
        }
        assert_eq(output.len(), 1)
        assert((output.get(0) ?? "").contains("Member"))
        assert(!((output.get(0) ?? "").contains("Outsider")))
    }
}

test "project and repo tag are orthogonal" {
    with db.connect(":memory:") {
        setup_test_db()
        db.exec("INSERT INTO projects (id, name) VALUES ('PROJ0001', 'launch')").unwrap()
        db.exec("INSERT INTO tasks (id, title, status, priority, project_id) VALUES ('TASK0001', 'Both', 'open', 2, 'PROJ0001')").unwrap()
        db.exec("INSERT INTO tags (task_id, tag) VALUES ('TASK0001', 'repo:bridge')").unwrap()

        let mut by_tag: List[Str] = []
        with capture_print(by_tag) {
            cmd_ls("", ["repo:bridge"], "", false, false, false)
        }
        assert_eq(by_tag.len(), 1)
        assert((by_tag.get(0) ?? "").contains("Both"))

        let mut by_proj: List[Str] = []
        with capture_print(by_proj) {
            cmd_ls("", [], "launch", false, false, false)
        }
        assert_eq(by_proj.len(), 1)
        assert((by_proj.get(0) ?? "").contains("Both"))
    }
}

test "cmd_ready shows project name when unfiltered" {
    with db.connect(":memory:") {
        setup_test_db()
        db.exec("INSERT INTO projects (id, name) VALUES ('PROJ0001', 'webapp')").unwrap()
        db.exec("INSERT INTO tasks (id, title, status, priority, project_id) VALUES ('TASK0001', 'Member', 'open', 2, 'PROJ0001')").unwrap()
        let mut output: List[Str] = []
        with capture_print(output) {
            cmd_ready([], "", false)
        }
        assert_eq(output.len(), 1)
        assert((output.get(0) ?? "").contains("Member"))
        assert((output.get(0) ?? "").contains("(webapp)"))
    }
}

test "cmd_ready suppresses project name when filtered" {
    with db.connect(":memory:") {
        setup_test_db()
        db.exec("INSERT INTO projects (id, name) VALUES ('PROJ0001', 'webapp')").unwrap()
        db.exec("INSERT INTO tasks (id, title, status, priority, project_id) VALUES ('TASK0001', 'Member', 'open', 2, 'PROJ0001')").unwrap()
        let mut output: List[Str] = []
        with capture_print(output) {
            cmd_ready([], "webapp", false)
        }
        assert_eq(output.len(), 1)
        assert((output.get(0) ?? "").contains("Member"))
        assert(!((output.get(0) ?? "").contains("(webapp)")))
    }
}

test "cmd_graph shows project name when unfiltered" {
    with db.connect(":memory:") {
        setup_test_db()
        db.exec("INSERT INTO projects (id, name) VALUES ('PROJ0001', 'webapp')").unwrap()
        db.exec("INSERT INTO tasks (id, title, status, priority, project_id) VALUES ('TASK0001', 'Member', 'open', 2, 'PROJ0001')").unwrap()
        let mut output: List[Str] = []
        with capture_print(output) {
            cmd_graph("", [], false, "")
        }
        assert(output.join("\n").contains("Member"))
        assert(output.join("\n").contains("(webapp)"))
    }
}

test "cmd_graph suppresses project name when filtered" {
    with db.connect(":memory:") {
        setup_test_db()
        db.exec("INSERT INTO projects (id, name) VALUES ('PROJ0001', 'webapp')").unwrap()
        db.exec("INSERT INTO tasks (id, title, status, priority, project_id) VALUES ('TASK0001', 'Member', 'open', 2, 'PROJ0001')").unwrap()
        let mut output: List[Str] = []
        with capture_print(output) {
            cmd_graph("", [], false, "webapp")
        }
        assert(output.join("\n").contains("Member"))
        assert(!(output.join("\n").contains("(webapp)")))
    }
}

test "project assignment does not affect ready/blocked DAG" {
    with db.connect(":memory:") {
        setup_test_db()
        db.exec("INSERT INTO projects (id, name) VALUES ('PROJ0001', 'launch')").unwrap()
        db.exec("INSERT INTO tasks (id, title, status, priority, project_id) VALUES ('BLOCKER1', 'Blocker', 'open', 1, 'PROJ0001')").unwrap()
        db.exec("INSERT INTO tasks (id, title, status, priority, project_id) VALUES ('BLOCKED1', 'Blocked', 'open', 2, 'PROJ0001')").unwrap()
        db.exec("INSERT INTO deps (blocker_id, blocked_id) VALUES ('BLOCKER1', 'BLOCKED1')").unwrap()
        let mut output: List[Str] = []
        with capture_print(output) {
            cmd_ready([], "launch", false)
        }
        assert_eq(output.len(), 1)
        assert((output.get(0) ?? "").contains("Blocker"))
        assert(!((output.get(0) ?? "").contains("Blocked")))
    }
}

test "escape_like_pattern leaves plain text untouched" {
    assert_eq(escape_like_pattern("abc123"), "abc123")
}

test "escape_like_pattern escapes percent" {
    assert_eq(escape_like_pattern("50%"), "50!%")
}

test "escape_like_pattern escapes underscore" {
    assert_eq(escape_like_pattern("a_b"), "a!_b")
}

test "escape_like_pattern doubles the escape char first" {
    assert_eq(escape_like_pattern("!%"), "!!!%")
}

test "try_resolve_id returns Ok on exact match" {
    with db.connect(":memory:") {
        setup_test_db()
        db.exec("INSERT INTO tasks (id, title, status, priority) VALUES ('abc123', 'One', 'open', 2)").unwrap()
        let expected: Result[Str, Str] = Ok("abc123")
        assert_eq(try_resolve_id("abc123"), expected)
    }
}

test "try_resolve_id returns Ok on unique prefix" {
    with db.connect(":memory:") {
        setup_test_db()
        db.exec("INSERT INTO tasks (id, title, status, priority) VALUES ('abc123', 'One', 'open', 2)").unwrap()
        db.exec("INSERT INTO tasks (id, title, status, priority) VALUES ('xyz789', 'Two', 'open', 2)").unwrap()
        let expected: Result[Str, Str] = Ok("abc123")
        assert_eq(try_resolve_id("abc"), expected)
    }
}

test "try_resolve_id returns Err on no match" {
    with db.connect(":memory:") {
        setup_test_db()
        db.exec("INSERT INTO tasks (id, title, status, priority) VALUES ('abc123', 'One', 'open', 2)").unwrap()
        let expected: Result[Str, Str] = Err("No task matches prefix: zzz")
        assert_eq(try_resolve_id("zzz"), expected)
    }
}

test "try_resolve_id returns Err on ambiguous prefix" {
    with db.connect(":memory:") {
        setup_test_db()
        db.exec("INSERT INTO tasks (id, title, status, priority) VALUES ('abc123', 'One', 'open', 2)").unwrap()
        db.exec("INSERT INTO tasks (id, title, status, priority) VALUES ('abc456', 'Two', 'open', 2)").unwrap()
        let expected: Result[Str, Str] = Err("Ambiguous prefix 'abc', multiple matches")
        assert_eq(try_resolve_id("abc"), expected)
    }
}

test "try_resolve_id lowercases the prefix before matching" {
    with db.connect(":memory:") {
        setup_test_db()
        db.exec("INSERT INTO tasks (id, title, status, priority) VALUES ('abc123', 'One', 'open', 2)").unwrap()
        let expected: Result[Str, Str] = Ok("abc123")
        assert_eq(try_resolve_id("ABC1"), expected)
    }
}

test "try_resolve_id does not treat percent as a wildcard" {
    with db.connect(":memory:") {
        setup_test_db()
        db.exec("INSERT INTO tasks (id, title, status, priority) VALUES ('abc123', 'One', 'open', 2)").unwrap()
        db.exec("INSERT INTO tasks (id, title, status, priority) VALUES ('xyz789', 'Two', 'open', 2)").unwrap()
        let expected: Result[Str, Str] = Err("No task matches prefix: %")
        assert_eq(try_resolve_id("%"), expected)
    }
}

test "try_resolve_id does not treat underscore as a single-char wildcard" {
    with db.connect(":memory:") {
        setup_test_db()
        db.exec("INSERT INTO tasks (id, title, status, priority) VALUES ('abc123', 'One', 'open', 2)").unwrap()
        let expected: Result[Str, Str] = Err("No task matches prefix: a_c")
        assert_eq(try_resolve_id("a_c"), expected)
    }
}

test "try_resolve_project_id resolves by exact name" {
    with db.connect(":memory:") {
        setup_test_db()
        db.exec("INSERT INTO projects (id, name) VALUES ('p1aaaa', 'webapp')").unwrap()
        let expected: Result[Str, Str] = Ok("p1aaaa")
        assert_eq(try_resolve_project_id("webapp"), expected)
    }
}

test "try_resolve_project_id resolves by id prefix" {
    with db.connect(":memory:") {
        setup_test_db()
        db.exec("INSERT INTO projects (id, name) VALUES ('p1aaaa', 'webapp')").unwrap()
        let expected: Result[Str, Str] = Ok("p1aaaa")
        assert_eq(try_resolve_project_id("p1a"), expected)
    }
}

test "try_resolve_project_id returns Err on ambiguous id prefix" {
    with db.connect(":memory:") {
        setup_test_db()
        db.exec("INSERT INTO projects (id, name) VALUES ('p1aaaa', 'webapp')").unwrap()
        db.exec("INSERT INTO projects (id, name) VALUES ('p1bbbb', 'api')").unwrap()
        let expected: Result[Str, Str] = Err("Ambiguous project 'p1', multiple matches")
        assert_eq(try_resolve_project_id("p1"), expected)
    }
}

test "try_resolve_project_id resolves a name containing an underscore" {
    with db.connect(":memory:") {
        setup_test_db()
        db.exec("INSERT INTO projects (id, name) VALUES ('p2xxxx', 'my_app')").unwrap()
        let expected: Result[Str, Str] = Ok("p2xxxx")
        assert_eq(try_resolve_project_id("my_app"), expected)
    }
}

test "is_task_id_taken is true for an existing id and false otherwise" {
    with db.connect(":memory:") {
        setup_test_db()
        db.exec("INSERT INTO tasks (id, title, priority) VALUES ('abc123', 'existing', 2)").unwrap()
        assert(is_task_id_taken("abc123"))
        assert(!is_task_id_taken("zzzzzz"))
    }
}

test "generate_unique_task_id returns a valid, unused id" {
    with db.connect(":memory:") {
        setup_test_db()
        let id = generate_unique_task_id()
        assert(is_valid_id(id))
        assert(!is_task_id_taken(id))
    }
}
